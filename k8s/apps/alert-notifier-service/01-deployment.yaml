apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-notifier-service
  namespace: dsbd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-notifier-service
  template:
    metadata:
      labels:
        app: alert-notifier-service
    spec:
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z kafka 9092; do echo 'attendo kafka...'; sleep 2; done"]
      containers:
        - name: alert-notifier-service
          image: dsbd/alert-notifier-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker

            # White-box monitoring: label richieste
            - name: SERVICE_NAME
              value: alert-notifier-service
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName

            # Actuator/Prometheus: esplicito via env per robustezza
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: health,info,prometheus
            - name: MANAGEMENT_ENDPOINT_PROMETHEUS_ENABLED
              value: "true"
            - name: MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED
              value: "true"

          envFrom:
            - configMapRef:
                name: dsbd-config
            - secretRef:
                name: dsbd-secrets

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3